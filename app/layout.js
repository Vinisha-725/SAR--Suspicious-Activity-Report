export const metadata = {
  title: 'SAR - Suspicious Activity Report',
  description: 'AI-powered Suspicious Activity Report system',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
